import {StyleSheet} from 'react-native';

export const LoadingStyleSheet = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  content: {},
});
