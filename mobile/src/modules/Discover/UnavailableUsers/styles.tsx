import {StyleSheet} from 'react-native';

export const UnavailableUsersStyleSheet = StyleSheet.create({
  wrapper: {
    minHeight: 400,
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
  },
  image: {
    width: 200,
    height: 200,
  },
});
