import {StyleSheet} from 'react-native';

export const ContactsHeaderStyleSheet = StyleSheet.create({
  wrapper: {
    padding: 15,
    justifyContent: 'center',
  },
  title: {
    color: 'white',
  },
});
