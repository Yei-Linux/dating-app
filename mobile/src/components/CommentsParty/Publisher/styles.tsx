import {StyleSheet} from 'react-native';

export const PublisherStyleSheet = StyleSheet.create({
  wrapper: {
    marginBottom: 40,
  },
  publisher: {
    backgroundColor: 'white',
    borderRadius: 15,
    padding: 10,
  },
  submit: {
    marginTop: 15,
  },
});
