import {StyleSheet} from 'react-native';

export const TyperStyleSheet = StyleSheet.create({
  wrapper: {
    backgroundColor: 'white',
    borderRadius: 10,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    overflow: 'hidden',
  },
  typer: {
    backgroundColor: 'white',
  },
});
